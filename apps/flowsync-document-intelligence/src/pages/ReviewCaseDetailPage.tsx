import { EmptyState } from "../components/EmptyState";

export function ReviewCaseDetailPage() {
  return <EmptyState title="Review case is not connected" message="Safe correction and decision summaries arrive in Phase 3." />;
}

