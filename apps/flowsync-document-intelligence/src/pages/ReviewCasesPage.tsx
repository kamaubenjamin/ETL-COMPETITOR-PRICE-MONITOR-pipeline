import { EmptyState } from "../components/EmptyState";

export function ReviewCasesPage() {
  return <EmptyState title="Review queue is not connected" message="Read-only review workload arrives in Phase 3." />;
}

