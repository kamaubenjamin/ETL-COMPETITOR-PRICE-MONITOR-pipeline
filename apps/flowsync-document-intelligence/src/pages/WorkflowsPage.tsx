import { EmptyState } from "../components/EmptyState";

export function WorkflowsPage() {
  return <EmptyState title="Workflow activity is not connected" message="Read-only workflow history arrives in Phase 3." />;
}

