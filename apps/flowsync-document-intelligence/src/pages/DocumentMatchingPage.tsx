import { EmptyState } from "../components/EmptyState";

export function DocumentMatchingPage() {
  return <EmptyState title="Matching data is not connected" message="Candidate confidence summaries arrive in Phase 3." />;
}

