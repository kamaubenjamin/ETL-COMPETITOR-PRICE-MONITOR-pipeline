import { EmptyState } from "../components/EmptyState";

export function AuditPage() {
  return <EmptyState title="Audit history is not connected" message="Privacy-safe audit events arrive in Phase 3." />;
}

